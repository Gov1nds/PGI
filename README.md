# Padanailath & Company — Website (Next.js + Tailwind)

Black theme, McKinsey-style mega menu, and content pages (News, Insights, Articles, Projects) designed for frequent updates.

## Quick start
```bash
npm install
npm run dev
```

## Deploy on Vercel
- Push this repo to GitHub
- Import in Vercel
- Build Command: `npm run build`

## Update content (no code needed)
Edit the files in `src/content/` and replace images in `public/images/`.

Each content list includes **two models**: `feature` and `standard`.
